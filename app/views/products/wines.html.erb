<div class="container">
  <h1>Hello from wines</h1>
  <div>
    <%= link_to('New wine', new_product_path) if vadmin? %>
  </div>
  <% @wines.each do |wine|  %>
    <%= link_to(wine.name, wine_path(wine)) %>
  <% end %>
</div>
